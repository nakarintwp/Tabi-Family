# V6.1 — Cartoon Blue Theme

ธีมนี้เปลี่ยนหน้าตา Tabi Family เป็นการ์ตูนโทนฟ้า-ขาว-เหลืองแบบสนุก สดใส และกลมมน โดยคงฟังก์ชันทั้งหมดจาก V6 เดิม

## แนวทางดีไซน์
- ฟ้า/ขาวเป็นสีหลัก
- เหลืองเป็น accent เล็กน้อย
- ไม่ใช้สีแดงเป็นสีหลัก
- Card และปุ่มทรงกลม/แคปซูล
- พื้นหลังมีฟองอากาศ เมฆ และประกายแบบ CSS
- ไม่มีรูปหรือโลโก้ตัวละครลิขสิทธิ์
- ไม่มี API หรือ asset ภายนอกเพิ่ม

## การอัปเกรด
ไม่ต้อง Run SQL เพิ่ม

Copy ไฟล์ทั้งหมดทับโปรเจกต์เดิม โดยเก็บ `.git` และ `.env.local` ไว้ แล้วรัน:

```powershell
git add .
git commit -m "V6.1 cartoon blue theme"
git push
```
